# 에이전트 설정 파일 예시
# 이 파일을 config.yaml로 복사하여 사용하세요.

# 서버 주소 (호스트:포트)
server_address: "localhost:8080"

# 상태 정보 수집 주기 (초)
status_interval: 5

# 업데이트 확인 주기 (초)
update_check_interval: 60

# 로그 파일 경로
log_file: "agent.log"
