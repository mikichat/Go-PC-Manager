# 서버 설정 파일 예시
# 이 파일을 config.yaml로 복사하여 사용하세요.

# 서버 포트
port: "8080"

# 정적 파일 디렉토리
static_dir: "static"

# 업데이트 파일 디렉토리
updates_dir: "updates"

# 현재 에이전트 버전
agent_version: "1.0.1"

# 인증 토큰 (보안) - 에이전트와 동일하게 설정하세요
auth_token: "your_secret_token_here"
